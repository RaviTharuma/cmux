name: Resolve nested-topology merge with upstream main

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  merge-upstream:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "Merge already landed; use apply-policy-10045.yml"
